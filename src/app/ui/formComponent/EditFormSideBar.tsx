import SideBarComponentButton from './button/SideBarCompontButton';

export default function EditFormSideBar() {
  return (
    <nav>
      <SideBarComponentButton componentType="PortfolioEditCardList">
        產生
      </SideBarComponentButton>
    </nav>
  );
}
